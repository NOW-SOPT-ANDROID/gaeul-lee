<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/pink"/> <!-- 분홍색 배경 -->

    <!-- Radius 설정 -->
    <corners android:radius="40dp"/>

    <!-- Padding 설정 -->
    <padding
        android:left="16dp"
        android:right="16dp"
        android:top="10dp"
        android:bottom="10dp"/>
</shape>